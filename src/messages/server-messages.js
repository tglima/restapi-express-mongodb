import { ENV_MODE, NU_PORT, URL_SWAGGER } from '../configs/config-manager';

export const MSG_START_SERVER = `Env mode: ${ENV_MODE}\nAPI listening on ${NU_PORT}\nSwagger link: ${URL_SWAGGER}`;
export const MSG_START_DB = 'Conexão com o banco de dados estabelecida com sucesso';
